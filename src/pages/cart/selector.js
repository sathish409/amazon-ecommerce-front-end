// import {createSelector} from '@reduxjs/toolkit'



// export  const SelectCartList = (state)=>state.productInfo.items

// export const selectSubTotal = createSelector(
//     [SelectCartList],
//     (cartList)=>cartList.reduce((acc, item)=> acc + item.price )
// )